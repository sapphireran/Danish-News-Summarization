# Almanac CSVs

Fiction. Regenerated by `python3 -m maalestok fixtures`.

The raw table uses `article text` because that is the column
`translate.py` reads. Later stages use `body` because that is what
`summary.py` / `finetune.py` read. The public-eval shape uses
`input_text` / `target_text` because that is what `eval.py` reads.
Those three names are the 2023 contract; they are not cleaned up here.
