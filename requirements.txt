# Core pipeline (translation, summarization, fine-tuning, evaluation).
# Pin loosely so a 2023-era course project still installs on current wheels.
# Use a CUDA-enabled torch build that matches your GPU driver if you train.

torch>=2.0.0
transformers>=4.30.0
accelerate>=0.20.0
sentencepiece>=0.1.99
protobuf>=3.20.0
ctranslate2>=3.16.0

pandas>=1.5.0
numpy>=1.24.0
tqdm>=4.65.0
nltk>=3.8.0

datasets>=2.12.0
evaluate>=0.4.0
rouge-score>=0.1.2
bert-score>=0.3.13

# Optional extras used by some Hugging Face tokenizers / metrics
sacremoses>=0.0.53
filelock>=3.12.0
pyyaml>=6.0
