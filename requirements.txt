# Loose personal pin list for the 2023 GPU scripts.
# The course lab image was not frozen here. Install versions that match
# your CUDA stack, then expect to edit use_auth_token / load_metric as
# described in docs/troubleshooting.md.
#
# The examples/scripts/ tools need only the Python standard library.

# --- silver-label factory ---
pandas>=1.5
nltk>=3.8
ctranslate2>=3.16
transformers>=4.30
torch>=2.0
tqdm>=4.65

# --- fine-tune + eval ---
datasets>=2.12
evaluate>=0.4
numpy>=1.23
# rouge and bertscore go through datasets.load_metric in the 2023 scripts.
# On a later rerun, prefer: pip install evaluate rouge-score bert-score
rouge-score>=0.1.2
bert-score>=0.3.13
absl-py>=1.4
sentencepiece>=0.1.99
protobuf>=3.20
